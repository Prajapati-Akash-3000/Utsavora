// Exact pixel dimensions per platform — exported image uses these so the card fills the frame (no extra whitespace)
export const CARD_SIZES = {
  INVITATION: {
    width: 1000,
    height: 1500,   // 2:3 (Poster / Card)
  },
  POST: {
    width: 1080,
    height: 1080,   // 1:1 (Instagram Post)
  },
  STORY: {
    width: 1080,
    height: 1920,   // 9:16 (Story / Status)
  },
};
